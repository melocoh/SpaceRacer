using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scrTester : MonoBehaviour
{
    public GameObject testServer;
    public GameObject testClient;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetMouseButtonDown(0))
        //{
        //    Instantiate(testServer, new Vector3(-1, 0, 0), Quaternion.identity);
        //}
        //if (Input.GetMouseButtonDown(1))
        //{
        //    Instantiate(testClient, new Vector3(1, 0, 0), Quaternion.identity);
        //}
    }


}
